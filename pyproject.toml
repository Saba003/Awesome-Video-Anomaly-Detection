[project]
name = "vad-survey-resource"
version = "0.1.0"
description = "Companion resources for a review of deep learning-based video anomaly detection"
requires-python = ">=3.10"
dependencies = [
  "pandas>=2.0",
  "matplotlib>=3.8",
  "pyyaml>=6.0"
]

[tool.ruff]
line-length = 100
